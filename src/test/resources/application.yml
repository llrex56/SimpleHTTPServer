spring:
  datasource:
    url: "jdbc:h2:mem:"
    username: sa
    password:
    driver-class-name: org.h2.Driver
    schema: classpath:db_unit_create.sql
    data: classpath:db_unit_data.sql

## Mybatis配置
mybatis:
  mapper-locations: classpath:mapper/*.xml

